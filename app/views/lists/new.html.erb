<div class="container">
  <%= simple_form_for [ @list ], :html => { :class => 'form-vertical' } do |f| %>
    <%= f.input :name %>
    <%= f.input :photo, as: :file %>
    <%= f.submit "Submit" %>
  <% end %>
</div>

